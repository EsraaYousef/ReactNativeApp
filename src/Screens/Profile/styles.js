import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    profileBlock: {
        flex: 1,
        backgroundColor: '#000',
        justifyContent: 'center',
        alignItems: 'center'
    },
    mainTitle:{
        color: '#fff',
        fontSize: 32
    }
})
export {styles}